version: '1'

services:
  api:
    image: tools_python:0.0.1
    volumes:
      - . .
    ports:
      - 8080:80
    
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "80"]
    